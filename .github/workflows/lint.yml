on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: nosborn/github-action-markdown-cli@53110ddff4a0a32df9abd4b609c688f20a0dbc56
        with:
          files: .
